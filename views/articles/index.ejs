<h1 class="text-center">All the articles you can handle</h1>
<% if(results.length <= 0){ %>
	<h2>Nothing in the database yet!</h2>
<% } else { %>
	<% results.forEach(function(item){ %>
		<a href="/articles/<%= item.id %>">
			<h2><%= item.title %></h2>
		</a>
	<% }); %>
<% } %>