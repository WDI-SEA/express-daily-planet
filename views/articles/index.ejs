<h1 class='text-center'>ARTICLES HOMEPAGE</h1>
<% if(results.length <= 0){%>
	<h2>Nothing in the db yet</h2>
<% } else { %>
	<% results.forEach(function(item){%>
		<%= item.title %>
	<% }); %>

<% } %>
