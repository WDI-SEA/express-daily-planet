<!DOCTYPE html>
  <html>
    <head>
      <title>All Articles</title>
    </head>
    <body>
      <div><% articles.forEach(function(article) { %>
        <h2><%= article.title %></h2>
          <p><%= article.body %></p>
      </div>
        <% }); %>
    </body>
  </html>
