<h1 class="text-center">Archive of Articles</h1>
<% if(results.length <= 0){ %>
	<h2> nothing in the database yet </h2>
<% } else { %> 
	<% results.forEach(function(item){ %>
		<%= item.title %>
	<% }); %>
<% } %>